---
name: KalaCUBE Deployment Pipeline
description: Docker Compose deployment foundation built for the rebuild — Dockerfiles, compose stack, CI. Config done, not yet deployed.
type: project
---

Built a **Docker Compose deployment foundation** for the KalaCUBE rebuild (the new `backend/` + `frontend/` monorepo apps). Chosen approach: Docker Compose (host-agnostic base), config-only first pass. Verified locally 2026-07-15; NOT yet deployed anywhere.

**Files added:**
- `backend/Dockerfile` — multi-stage NestJS image (deps → build → slim runtime), built from **monorepo root** context so npm workspaces resolve.
- `frontend/Dockerfile` — multi-stage Next.js **standalone** image (also root context). Standalone `server.js` lands at `frontend/.next/standalone/frontend/server.js`.
- `docker-compose.yml` — services: `backend` (:8000), `frontend` (:3000), `mongo` (:27017), `redis` (:6379). Compose injects `MONGO_URI=mongodb://mongo:27017/kalacube`, `REDIS_HOST=redis`. Named volumes `mongo_data`/`redis_data`.
- `.env.docker.example` → copy to `.env.docker` (git-ignored). AWS_* and EMAIL_* are Joi-`required`, so placeholders are shipped so the API boots.
- `.dockerignore`, `.github/workflows/ci.yml` (lint + build both apps + build both images w/ GHA cache), `DEPLOYMENT.md`.

**Incidental fixes required (both real, both verified):**
- `backend/nest-cli.json` — added `assets` so email `.hbs` templates copy into `dist/email/templates` (mailer reads `join(__dirname,'templates')`). Was broken for `start:prod` AND Docker before.
- root `package.json` — added `"packageManager": "npm@10.8.2"` (Turbo 2.9 refused to run without it).

**Verified:** `npm run build:backend` ✓ (templates in dist), `npm run build:frontend` ✓ (standalone path correct), `turbo run lint` resolves. **Docker images NOT built** — Docker Desktop daemon was off. To test end-to-end: start Docker Desktop → `cp .env.docker.example .env.docker && docker compose up --build`.

**Known debt:** 3 pre-existing frontend lint errors (`no-explicit-any` in `profile/[username]/page.tsx` and `profile/edit/page.tsx`). CI lint step set `continue-on-error: true` so first CI run stays green; flip to blocking once cleaned. Also note: `main.ts` has NO global `/api` prefix but OAuth callback env URLs assume `/api/...` — pre-existing inconsistency, not yet addressed.

**LOCAL E2E RUNNING (2026-09-09):** full stack up via `docker compose up --build -d`. Ports remapped in `./.env` (git-ignored) because host has clashes: `FRONTEND_PORT=3005` (3000/3001 taken by other node apps), `MONGO_PORT=27018` (host has a NATIVE mongod on 27017 too — big gotcha). Compose ports now use `${FRONTEND_PORT:-3000}` / `${MONGO_PORT:-27017}`. Frontend Dockerfile + compose take NEXT_PUBLIC_COGNITO_* build args. `.env.docker` created (git-ignored) with live Cognito values. Migration run into CONTAINER mongo via `TARGET_URI=mongodb://localhost:27018/kalacube node .secrets/migrate.js` (`.secrets/migrate.js` = the write-mode migration, copy-only, preserves _id). Backend (internal `mongo:27017`) serves it. Verified: 478 users/481 profiles/2123 artworks; real artist `arthouseecstasy` (vatsala428@gmail.com) has 53 artworks w/ correct S3 imagePrefix `protected/{identityId}/artist_work/{artworkId}/`. Backend guard 401s w/o token; Cognito verifier ready. **To log in for testing:** AWS session must create a Cognito user with a REAL migrated email (e.g. vatsala428@gmail.com) + known password. Images NOT wired yet (need S3). ⚠️ CLEANUP: first migration attempt accidentally wrote a `kalacube` db into the user's NATIVE mongod (localhost:27017, alongside dinetist-media/hotel-crm-dev) — offer to drop it.

**DEV WORKFLOW (2026-09-09, switched from Docker rebuilds — too slow for iteration):** Docker runs ONLY mongo(27018)+redis(6379). Backend+frontend run as HOST dev servers (hot reload):
- Backend: `MONGO_URI="mongodb://localhost:27018/kalacube" npm run dev:backend` (nest watch, :8000, connects container mongo+redis, Cognito live). 
- Frontend: `PORT=3005 npm run dev:frontend` (next dev, :3005, reads frontend/.env.local).
Docker backend/frontend containers are STOPPED (their images were stale). For a prod-parity check, rebuild images later (`docker compose up --build`, slow ~10min).

**REBUILD PAGES (revamp, matching original site map [[reference_original_site_map]]):** Built public browse API `/api/explore/{artists,artworks}` (ExploreModule + artwork.schema.ts mapping `artworks` collection; returns `{items,total,page,limit}` — note ResponseInterceptor unwraps a `data` key so DON'T name the payload key `data`). Frontend pages built + verified serving real data: `/` (cinematic home: hero + 3 dimensions + featured artists + recent artworks), `/all-artist` (478 artists grid), `/explore` (2123 artworks gallery). Style: user asked for LIGHT theme (2026-09-09, NOT the dark cinematic muse.tv vision) — switched all pages+header to light: bg `#faf8f5`, text neutral-900, borders neutral-200, gold accent darkened to `#a06f1e` for contrast, gold fills/borders `#cda45c` kept, serif headings. Images = placeholders (S3 not wired). Also built + verified: global dark **Header** nav (Home/Explore/Artists + Sign in) mounted in layout.tsx; `/artist/[username]` (artist detail: bio, dimensions, socials, their works) + `/art-work/[id]` (artwork detail) via public `/api/explore/artists/:username` and `/api/explore/artworks/:id`. Fixed CORS in main.ts (allow any localhost port in non-prod — frontend on :3005 was blocked by hardcoded :3000). Full nav works: home→explore→artist→artwork. Harmless hydration warning in console is from a browser extension (`data-scribe-recorder-ready`), not our code. NOT built yet: `/all-categories`, `/art-space`, `/events`, images (S3). All on branch `revamp`, uncommitted.

**FEATURES BUILT (2026-09-09, on `revamp`, dev servers hot-reload):** global Footer; artist filters (dimension + search, `/api/explore/artists?dimension=&search=`); world-class animated home (framer-motion: auto-scrolling artwork marquees `ArtworkMarquee.tsx`, scroll-in sections, hover-lift cards) + "Our Story" origin section (Musée Art Café 2019 → KalaCUBE born in COVID 2020 → today); artist avatars rendered everywhere.
**ADMIN PANEL (built):** AdminModule `/api/admin/*` (@Roles('admin','superadmin')): stats, users list+update(role/isActive), artworks list+update(status)+delete, artspaces, events. Frontend `/admin` (Overview stats + Users table w/ role select + active toggle + Artworks table w/ status + delete). **Admin user = musee.initialize@gmail.com (username `devteam`) promoted to superadmin in local mongo.** To ACCESS: log in via email/password (needs Cognito password set: `aws cognito-idp admin-set-user-password --user-pool-id ap-south-1_6Tz4OLn4d --username musee.initialize@gmail.com --password 'Admin1234!' --permanent --region ap-south-1`).
**LOGIN FIX:** "Continue with Google" button HIDDEN on login (gated behind `NEXT_PUBLIC_GOOGLE_LOGIN=true`) — it caused persistent Cognito `redirect_mismatch` (callback for :3005 not whitelisted + Google IdP not enabled). Email/password (Amplify SRP) works without Hosted UI once a password is set. Re-enable Google later after AWS adds :3005 callback + Google IdP.
**Commits on `revamp`:** 593e49a (rebuild pages), + footer/filters/home-animation commit. Admin+story NOT yet committed.
**STILL PENDING:** art-collection page+filters, richer artist profile, QR codes (tasks 16/17/19).

**PROD-PARITY DOCKER ATTEMPT (2026-09-09, session on `revamp`):** User asked to run the full stack in Docker (`docker compose up --build`). **Frontend image build FAILED: `next build` runs a full TypeScript typecheck that `next dev` SKIPS** — so type errors only surfaced at prod build time. The offender: `frontend/src/app/page.tsx` `fadeUp` framer-motion variant had `ease: 'easeOut'` which TS widened to `string`, not the required `Easing` literal (8 cascading errors, all from that one const). **FIX: `import { motion, type Variants }` and annotate `const fadeUp: Variants = {...}`** — contextual typing narrows the ease string. Host `npx tsc --noEmit -p tsconfig.json` in `frontend/` then clean; admin & blog pages had NO type errors. LESSON: run `tsc --noEmit` on host BEFORE slow Docker rebuilds to catch all type errors at once. **Backend image builds fine.** The Docker prod build is SLOW (~10min, npm-install layers re-run on cache invalidation) and frustrating for iteration — **abandoned prod-parity for now; reverted to the fast HOST DEV workflow** (mongo:27018+redis:6379 in Docker, backend `MONGO_URI=mongodb://localhost:27018/kalacube npm run dev:backend` :8000, frontend `PORT=3005 npm run dev:frontend` :3005; logs in scratchpad backend.log/frontend.log). Docker prod-parity + cloud deploy still TODO. NOTE: the `page.tsx` type fix is a real uncommitted change now needed for ANY prod build.

**PRODUCTION LAUNCH PLAN (2026-09-09, DECISIONS LOCKED, supersedes old Render/Railway idea):**
- **CORRECTION about current prod:** `kalacube.com` is NOT "Coming soon" — it is a **FULL LIVE React SPA (Create React App `kala-cube`)** served from **S3 + CloudFront** (serverless), with a **working admin panel** (`/admin/featured-artist` → 200). Backend `api.kalacube.com` → single **EC2** box `52.66.44.237` (ap-south-1). So the revamp REPLACES a working product; new admin must cover old admin features (featured-artist etc.). (Memory's "bare Coming soon starter" referred only to the legacy GitHub repo, not the deployed site.)
- **User decisions:** (1) launch scope = **FULL end-to-end, THEN cut over** (build signup-onboarding + artwork-upload, test signup→profile→upload, then flip) — NOT a browse-only soft launch. (2) hosting = **stay on AWS, ALL SERVERLESS, no Docker, git-push deploys** ("like what we have now"). (3) wants fast iteration / no build delay.
- **Target architecture:** Frontend Next.js → **AWS Amplify Hosting (Gen 2)** (git push → CloudFront in ~2-3min, preview branches, serverless SSR). Backend NestJS → **AWS Lambda + API Gateway** via `serverless-express` adapter (deploy w/ SST or Serverless Framework; replaces EC2). DB → **MongoDB Atlas Serverless**, fresh writable `kalacube` (migrated copy; original `05_202011_kalaCUBE` untouched). Auth → **Cognito** existing pool `ap-south-1_6Tz4OLn4d` (+prod callbacks). Storage → **S3** `05-202011-kalacube-artist` (+authenticated write for uploads). DNS → **Route53** repoint; keep old EC2 as rollback.
- **BUILD-BEFORE-LAUNCH gaps (write path):** current backend HAS profile-edit endpoints (`PUT /users/:username/artist-profile`), admin, and an S3 `uploadFile`(PutObject)+presign service; MISSING = (a) Lambda adapter, (b) onboarding (create Mongo profile on first Cognito login — `isNewUser`/pendingProfile not built), (c) **artwork create/upload API + UI (net-new, biggest piece)**, (d) admin parity w/ old featured-artist, (e) explore pagination + category browse (user-reported gap), (f) full E2E write test. Local dev Mongo (27018) is a R/W COPY; Atlas source was read-only → prod needs writable Atlas.
- **AUTH STATUS:** login (Cognito SRP) WORKS (`devteam`/`Admin@1234`); forgot-password/reset WORKS via Amplify (memory's "broken forgot-password" now resolved). Login form field changed to accept USERNAME (pool has NO email alias — `UsernameAttributes:None`), so all 478 migrated users log in by legacy username, not email — consider email→username lookup before signIn for UX.
- **PHASE-1 SNAPSHOT:** commit `5ebfe37` on `revamp`, tagged **`phase-1`** (admin panel, blog, story, animated home, footer, filters, login username field, framer-motion prod-build type fix). `git checkout phase-1` restores it. NOT pushed to remote (`origin` = github.com/pratyada/kalacube).

**3-AGENT BUILD DONE (2026-09-10, on `revamp`, all typecheck clean, all UNCOMMITTED):**
- **Admin dashboard (industrialized):** rebuilt `frontend/src/app/admin/page.tsx` as left-sidebar dashboard + new `frontend/src/components/admin/*` (DashboardSection, ArtistsSection, ArtworksSection, FeaturedSection, EmailSection, SimpleTableSection, types.ts). Backend: added `isFeatured` to user.schema; AdminService/Controller gained richer stats (newSignups30d, artworksByStatus[], topDimensions[], featured), `GET /featured`, `GET /recipients?segment=all|active|has-artwork|dimension:<x>`, `POST /campaigns` ({subject,bodyHtml,segment,testEmail?,confirm?} — testEmail=preview-only; bulk requires confirm:true, caps >1000, writes `campaigns` collection), `GET /campaigns`. EmailService `sendNewsletter()` + `email/templates/newsletter.hbs`. AdminModule imports EmailModule. All routes mapped + 401-guarded (verified). **Email = Bull/Redis queue → Gmail SMTP; NEVER ran a real bulk send.**
- **Backend serverless scaffold:** `backend/src/lambda.ts` (@codegenie/serverless-express, warm-cached app), `backend/serverless.yml` (SF v3, Node20 arm64, HTTP API, secrets via SSM `/kalacube/prod/*`), scripts `build:lambda`/`deploy`, deps added to backend/package.json. Minimal `app.module.ts` edit (Mongoose pool/reuse + optional Redis TLS/password) + `config.schema.ts` (+REDIS_PASSWORD/REDIS_TLS). `main.ts` untouched (EC2 rollback intact). Runbook `DEPLOYMENT_BACKEND_SERVERLESS.md`. **Redis decision: keep Bull + Upstash serverless Redis for launch; migrate to SQS+worker Lambda later.** Caveats: Atlas must allow 0.0.0.0/0 (Lambda VPC-less); monorepo node_modules packaging is fiddly; cold starts 1-3s.
- **Frontend serverless scaffold:** `amplify.yml` (Amplify Gen2 monorepo, appRoot=frontend), `frontend/next.config.ts` edit (outputFileTracingRoot + `output:standalone` now opt-in via BUILD_STANDALONE — Dockerfile sets it), runbook `DEPLOYMENT_FRONTEND_SERVERLESS.md`. Prod build verified: `npm run build --workspace=frontend` exit 0, required-server-files.json present. Env table + Cognito callback (`https://kalacube.com/auth/oauth/callback`) documented.
- **GOTCHA HIT:** the backend agent's `npm install` + a Docker Desktop shutdown wedged both dev servers + killed mongo/redis containers; fixed by restarting Docker (`open -a Docker` → `docker compose up -d mongo redis`, images exist so instant) + restarting host dev servers. npm-workspace deps hoist to ROOT node_modules (backend/node_modules empty is NORMAL). Nest EXITS if Mongo down at boot (no retry) — start mongo BEFORE backend.
- **GO-LIVE CHECKLIST (human/AWS steps, nothing deployed yet):** (1) `git push -u origin revamp`; (2) provision Atlas (fresh writable `kalacube`, run write-migration, allow 0.0.0.0/0) + Upstash Redis; (3) put secrets in SSM `/kalacube/prod/*`; (4) backend: `cd backend && npm run build:lambda && npm run deploy` → map api.kalacube.com (ACM+APIGW+Route53); (5) frontend: create Amplify app (monorepo root `frontend`, branch revamp), set NEXT_PUBLIC_* env, attach kalacube.com+www; (6) update Cognito callbacks; (7) build STILL-MISSING write features (onboarding on first login, artwork upload API+UI) before full-self-serve launch; (8) flip Route53, keep EC2+old CloudFront as rollback.

**WRITE FEATURES BUILT (2026-09-10, `revamp`, backend+frontend typecheck clean, UNCOMMITTED):**
- **Onboarding:** guard (`jwt-auth.guard.ts`) already produced `{email,cognitoSub,isNewUser:true}`+`req.cognito` claims for new Cognito users; added the missing piece — `POST /api/auth/onboarding` (auth.service.onboard + `dto/onboarding.dto.ts` + `common/decorators/cognito-claims.decorator.ts`), idempotent, creates User(role artist)+artistprofile from claims+body. `/api/auth/me` now returns `{isNewUser:true}` instead of 404. Added `cognitoSub` (sparse index) to user.schema. Frontend `/onboarding` page (prefills from localStorage `pendingProfile` stashed by register); login routes `isNewUser` → /onboarding; User type gained `isNewUser?`/`isFeatured?`.
- **Artwork upload:** new `backend/src/artwork/` module — `POST /api/artworks` (multipart FilesInterceptor('images',8) → S3 `uploadFile` under `protected/{legacyIdentityId||userId}/artist_work/{id}/`, public-read via existing bucket policy), `GET /api/artworks/mine`, `PATCH/DELETE :id` (owner-checked). Reuses Artwork schema. Frontend `/dashboard/upload` page (form + multi-image + previews → multipart). Header dropdown has "+ Upload artwork". Fixed `S3Service.getPublicUrl` → regional URL-encoded form. NOTE: multipart-through-Lambda has ~6MB limit — switch to presigned-PUT + bucket CORS for prod scale (documented as follow-up).
- **NOT yet tested end-to-end live** (need a real new Cognito signup to walk signup→onboarding→upload→appears in explore).

**ADMIN NOC REDESIGN (2026-09-10, user wants "black CLI / space network operations dashboard, monitor everything"):** Rebuilt `/admin` as a FULL-SCREEN DARK mission-control console. New shared design system `frontend/src/components/admin/console.tsx` (`ui` class tokens + `Panel`,`StatTile`,`StatusDot`,`Meter`,`cx`; near-black `#05080c`, mono, terminal-green `#3ef2a1`/amber `#f5c451`/cyan `#5cc8ff`, borders `#132030`/`#1b2c3d`). Shell `app/admin/page.tsx` = top status bar (live UTC clock + API/DB/QUEUE LEDs) + terminal sidebar (`root@kalacube:~#`, section codes SYS/USR/ART/COMM…, clearance badge). `DashboardSection` = live telemetry (auto-refresh 15s, NOMINAL/DEGRADED + synced-Ns-ago, glowing StatTiles, Meter distributions). All 5 other sections restyled to the tokens. **Global Header + Footer now return null on `pathname.startsWith('/admin')`** (Footer converted to client component) so admin is immersive full-black. All typecheck clean; /admin,/onboarding,/dashboard/upload,/dashboard,/ all serve 200.

**E2E WRITE-PATH TESTING DONE (2026-09-10, headless via CLI Cognito token — the app client `6n3t3poh0co772eaahgvopcfn` ALLOWS `ADMIN_USER_PASSWORD_AUTH`, so `aws cognito-idp admin-initiate-auth ... --query AuthenticationResult.IdToken` yields a usable Bearer token for curl tests without a browser):**
- **Artwork upload VERIFIED:** POST /api/artworks (multipart) → 201, image PUT to real S3 `05-202011-kalacube-artist` under `protected/{legacyIdentityId}/artist_work/{id}/`, public URL returns HTTP 200 image/png, shows in /api/artworks/mine AND public /api/explore/artworks. DELETE/cleanup works.
- **Onboarding VERIFIED:** created throwaway Cognito user via `admin-create-user --message-action SUPPRESS` + `admin-set-user-password --permanent` → /api/auth/me returns `{isNewUser:true}` → POST /api/auth/onboarding 201 (creates artist user+profile, links cognitoSub) → /me returns full user. Cleaned up (deleted cognito + mongo user + profile).
- **BUGS FOUND & FIXED during testing:** (1) `Header.tsx` had the `/admin` early-return BEFORE its useEffects → "Rendered more hooks" crash ("looks broken") → moved return AFTER all hooks. (2) `ArtworkService.ownedOrThrow` compared ObjectId `art.artist.toString()` vs `@CurrentUser('_id')` which is an ObjectId → always 403 on PATCH/DELETE → fixed with `String(a)!==String(b)`. (3) `UserRepository.upsert{Artist,Curator,ArtSpace}Profile` stored `user` as a STRING on upsert-insert → breaks explore `$lookup` (ObjectId vs string) so onboarded artists' dimensions/headline wouldn't show → fixed with `new Types.ObjectId(userId)`. (4) S3 was silently 500ing: global `HttpExceptionFilter` didn't log non-HTTP errors (added Logger.error for ≥500) AND `backend/.env` had placeholder AWS creds + wrong bucket `kalacube-dev`.
- **S3 FIX (local dev):** `S3Service` now falls back to the DEFAULT AWS credential provider chain when env creds look like placeholders (uses the local `aws` config / Claude_sudo creds in dev, IAM role on Lambda) — also the right pattern for prod. `backend/.env` `AWS_STORAGE_BUCKET` changed `kalacube-dev`→`05-202011-kalacube-artist` (real public bucket; local writes now land in the real bucket — fine for now since we also READ migrated images from it; revisit for a dedicated staging bucket). Claude_sudo IAM confirmed has PutObject/DeleteObject on that bucket.
- **Cognito password:** devteam reset to `Admin@1234` (permanent). Repeated failed logins can lock the account and surface as "incorrect password" — re-run admin-set-user-password to clear.
- All backend+frontend typecheck clean. STILL UNCOMMITTED. Not yet browser-walked (extension was disconnected) but fully curl-verified.

**LOGIN "incorrect password" ROOT CAUSE + FIX (2026-09-10, browser-verified):** Amplify is configured with `{ ssr: true }` (frontend/src/lib/amplify.ts) → it stores the Cognito session in COOKIES, not localStorage (that's why localStorage only had a stale legacy HS256 `accessToken`, no CognitoIdentityServiceProvider keys). After a successful login the session persisted, but the zustand store reset on reload and showed logged-out. Re-attempting login then threw `UserAlreadyAuthenticatedException: There is already a signed in user`, which the login page mislabeled as "Incorrect email or password". FIXES: (1) `AmplifyProvider.tsx` now calls `fetchUser()` in a mount useEffect → the app HYDRATES from the existing cookie session on load (header shows the user automatically, no re-login needed). (2) `authStore.loginWithEmail` catches `UserAlreadyAuthenticatedException` and just `fetchUser()`s + returns (do NOT call `signOut()` first — with oauth configured it triggers a redirect that reloads the page and aborts the flow; that was a wrong first attempt). Browser-confirmed: logged in as Museee initialize (musee.initialize@gmail.com, SUPERADMIN), avatar dropdown works, /admin NOC console renders live data (478 users/2123 artworks/50 events). DIAGNOSIS TIP: to see the real Amplify error, temporarily `console.error(err?.name, err?.message)` in the login catch — the page's generic "Incorrect email or password" hides it.
**Also:** ArtworkService.create now rolls back (deletes the doc) if S3 upload throws — the earlier 500s (placeholder creds) had left image-less orphan artworks (cleaned). devteam Cognito password = `Admin@1234`. Frontend uses same client `6n3t3poh0co772eaahgvopcfn`/pool as CLI tests; client allows USER_SRP_AUTH + ADMIN_USER_PASSWORD_AUTH (latter enables headless token grab for curl tests).

**BACKEND IS LIVE ON AWS LAMBDA (2026-09-10/11 — staging, WORKING):** endpoint `https://nmk8io3ru5.execute-api.ap-south-1.amazonaws.com`, stack `kalacube-api-staging`, stage `staging`, region ap-south-1. Serves real Atlas data (2123 artworks, categories, images). Cold start ~5s.
- **Atlas prod DB:** user created a password DB user (Option A); writable SRV URI in `.secrets/atlas_rw.uri` (git-ignored; reconstructed from user+pass the user pasted into the Google client_secret json in docs/ — extracted it, cleaned the json back to valid, verified conn). New DB `kalacube` on `cluster0.b5uay.mongodb.net`, loaded via `mongodump` local kalacube → `mongorestore` (3383 docs). Original `05_202011_kalaCUBE` untouched. Atlas network access allows 0.0.0.0/0.
- **SSM params** under `/kalacube/prod/*` (15): MONGO_URI/JWT_*/EMAIL_PASSWORD = SecureString; S3 via Lambda IAM role (serverless.yml grants s3 on bucket); AWS_ACCESS_KEY/AWS_SECRET_KEY NOT set (reserved Lambda names → made optional in config.schema); REDIS_HOST/EMAIL_* = staging placeholders (email inert, fine — no email triggered by browse/login/upload).
- **DEPLOY GOTCHAS SOLVED (critical for any redeploy):** (1) `serverless.yml` must NOT set AWS_ACCESS_KEY/AWS_SECRET_KEY (reserved). (2) prod node_modules MUST be `cd backend && npm install --omit=dev --install-links --workspaces=false --os=linux --cpu=arm64` — the `--os/--cpu` gets Linux binaries for native deps (bcrypt, @css-inline via mailer); else Lambda crashes "Cannot find module ...-linux-arm64-gnu". Let install FULLY finish (~1min/479pkgs) else "Cannot find module 'date-fns'". (3) build via stash-dance: `mv node_modules node_modules.prod && ../node_modules/.bin/nest build && mv node_modules.prod node_modules && cp src/email/templates/*.hbs dist/email/templates/` (root nest binary, not npx). (4) logs: `aws logs filter-log-events --log-group-name /aws/lambda/kalacube-api-staging-api` (get-log-events breaks on `[$LATEST]` stream names). (5) failed stack delete: empty the ServerlessDeploymentBucket first.
- Cognito client `6n3t3poh0co772eaahgvopcfn` allows ADMIN_USER_PASSWORD_AUTH → headless test tokens via `aws cognito-idp admin-initiate-auth`.

**PRODUCTION SCOPE CLARIFIED (2026-09-10):** user: "no one currently uses kalacube.com, even if broken no one is using it" — so deploy STRAIGHT to production, breaking-live is a non-issue. Goal = fully-functional site: explore, login, view artworks, upload/delete artworks, update profile. (Supersedes the earlier cautious staging-then-cutover gating.)

**BRAND RE-THEME (2026-09-10):** user added official brand guide at `docs/visual brand guide/KalaCUBE_Brand_Guideline_Claude_Handoff.html` + logos (KalaCUBE_Logo_{Transparent,Dark_Background,Monochrome_Navy}.png) + 2 ChatGPT brand images. Palette: navy #0B1F52/#202F9A/#071738, cream #FAF7F2, accents yellow #FFD200 / orange #FF8A00 / magenta #D81B60 / teal #00A896, font **Inter** (replaces current gold+serif). Agent re-theming whole public site; admin dark console stays.

**PRODUCTION LAUNCH — FRONTEND ON AMPLIFY + kalacube.com CUTOVER (2026-09-11):**
- **Amplify app `kalacube-revamp` (appId `d2dp26a5omh9kk`)** — created via CLI using the `gh auth token` (pratyada, repo scope) — NO console OAuth needed. Connected to github.com/pratyada/kalacube, branch `revamp`, monorepo root `frontend`, WEB_COMPUTE (Next SSR). Env: NEXT_PUBLIC_API_URL=https://api.kalacube.com, APP_URL=https://kalacube.com, COGNITO_* set, GOOGLE_LOGIN=false, AMPLIFY_MONOREPO_APP_ROOT=frontend. First build SUCCEEDED. Live at `revamp.d2dp26a5omh9kk.amplifyapp.com`.
- **CORS FIX (critical):** backend main.ts/lambda.ts CORS only allows CLIENT_URL origins in prod → the amplify origin was rejected → 500 (`Origin ... not allowed by CORS`). Fixed by setting CLIENT_URL to a comma list `https://kalacube.com,https://www.kalacube.com,https://revamp.d2dp26a5omh9kk.amplifyapp.com` — updated BOTH the live Lambda env (`aws lambda update-function-configuration`, instant) AND SSM `/kalacube/prod/CLIENT_URL` (persists across redeploys). (allowed = CLIENT_URL.split(',')).
- **FULL E2E VERIFIED IN PRODUCTION (browser on the amplify URL → api.kalacube.com → Atlas → S3):** explore browse + images + category ladder ✓; login (Cognito SRP, devteam/Admin@1234) → dashboard ✓; authenticated upload (multipart POST → 201 + public S3 image) ✓; appears in /mine ✓; profile update PUT → 200 ✓; delete → 200, gone ✓. Test data cleaned (Atlas 2123, S3 object removed). Cognito idToken lives in cookie `CognitoIdentityServiceProvider.<clientId>.<user>.idToken` (ssr:true) — used it for the JS-driven upload test (the file_upload MCP tool no longer accepts host paths).
- **kalacube.com CUTOVER (2026-09-11):** `aws amplify create-domain-association` for kalacube.com (root+www → revamp). Amplify CloudFront target = `d2dcfd11m6xgyg.cloudfront.net`. Added to Route53 zone `Z02921781C5L2ZJPCCMIH`: cert-validation CNAME `_38a427da...kalacube.com`→`_ebf37...acm-validations.aws`; apex `kalacube.com` A-ALIAS → new CloudFront (HostedZoneId Z2FDTNDATAQYW2); www A-ALIAS → new CloudFront (www had to be A-ALIAS not CNAME — conflicted with existing A). **ROLLBACK:** apex+www were A-ALIAS → OLD CloudFront `d2hqaleog4pppu.cloudfront.net` (still exists); UPSERT back to roll back. MX (zoho email) untouched. Cert validation + domain AVAILABLE takes ~15-30min; until then HTTPS to kalacube.com may cert-error (no traffic, fine).
- **Note:** api.kalacube.com already existed (ACM cert + Route53 alias to API Gateway); Agent 2 remapped it from a stale backend to the working Lambda `nmk8io3ru5`.

**★ LIVE IN PRODUCTION (2026-09-11) — kalacube.com is the relaunched site. Full as-built reference: repo-root `DEPLOYMENT.md` (AWS/Amplify/Lambda/Atlas/Cognito/S3/Route53/GA — all IDs + deploy/rollback commands).** Quick facts: Amplify app `d2dp26a5omh9kk` (revamp branch, CloudFront `dctuy46slnri9`), Lambda stack `kalacube-api-staging` at api.kalacube.com, Atlas DB `kalacube`, Cognito `ap-south-1_6Tz4OLn4d`/client `6n3t3poh0co772eaahgvopcfn`, S3 `05-202011-kalacube-artist`, Route53 zone `Z02921781C5L2ZJPCCMIH`. Frontend deploy = `git push origin revamp` (auto-build). Backend deploy = manual serverless (see DEPLOYMENT.md §2).
**Shipped post-cutover (all live):** simplified signup (Artist/Guest only, no dob/phone, Google button removed, auto-onboard; backend onboarding accepts role artist|user); mobile-responsive (Header hamburger w/ fixed logo, scrollable filter ladders, tap-to-upload drop-zone); Journal filters+search; pages /all-categories,/events,/faqs; branded transactional emails (reset/verify/welcome/newsletter, email-safe); new cube favicon; full SEO/AEO (per-page meta+OG images via next/og + JSON-LD + sitemap.ts + robots.ts); **Google Analytics 4 `G-TP8KDLQR1Z` (stream 15757761751) in root layout via next/script beforeInteractive so it's in SSR HTML (afterInteractive was invisible to Google's tag detector)** + Core Web Vitals→GA4 (WebVitals.tsx). Commits pushed through `e070978`.
**Still deferred (need user):** Google login (Cognito Google IdP + callback URLs); email/newsletter activation (needs Upstash Redis in SSM REDIS_*); Google Search Console (submit kalacube.com/sitemap.xml).

**Deploy target decision (SUPERSEDED — see PRODUCTION LAUNCH PLAN above):** earlier idea was guided Render/Railway + managed MongoDB Atlas + Redis. Replaced by the all-serverless-AWS plan.

**BRANCHING (2026-09-08):** ALL revamp work lives on branch **`revamp`** (first revamp commit `04f4504`). **`main` = untouched original** (only `9de4cf7`). User wants the revamp kept as a totally separate track from the original code. Not pushed to remote yet. Secrets (.env, .secrets/, docs/client_secret_*.json) are git-ignored and were verified absent from the commit. See [[feedback_build_approach]] and [[project_kalacube_overview]].
